# =====================================================
# Supabase Configuration
# =====================================================

# Supabase Project URL (API endpoint)
# This is your project's public API endpoint
# Format: https://<project-ref>.supabase.co
# Find: Supabase Dashboard > Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=

# Supabase Anonymous Key (public, safe to expose in browser)
# Used for client-side requests with RLS enabled
# Find: Supabase Dashboard > Settings > API > anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase Service Role Key (secret, server-side only)
# Bypasses RLS - use only in trusted server environments
# Find: Supabase Dashboard > Settings > API > service_role key
# NEVER commit this to git or expose in client bundles
SUPABASE_SERVICE_ROLE_KEY=

# =====================================================
# Database Monitoring (Optional)
# =====================================================

# Direct database URL for migrations and monitoring scripts
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
# Find: Supabase Dashboard > Settings > Database > Connection string > Direct connection
# Note: Use port 5432 for migrations, 6543 for pooled connections
SUPABASE_DB_URL=

# Health check secret (optional, for protecting /api/health/db endpoint)
HEALTH_CHECK_SECRET=

# Connection monitoring threshold (default: 80)
# Alert when active connections exceed this percentage of max_connections
CONNECTION_THRESHOLD=80

# Slow query threshold in milliseconds (default: 1000)
# Queries exceeding this threshold will be flagged in performance reports
SLOW_QUERY_MS=1000

# =====================================================
# Stripe Configuration
# =====================================================

# Stripe Secret Key (server-side only)
# Find: Stripe Dashboard > Developers > API keys > Secret key
STRIPE_SECRET_KEY=

# Stripe Webhook Secret (for webhook signature verification)
# Find: Stripe Dashboard > Developers > Webhooks > Signing secret
STRIPE_WEBHOOK_SECRET=

# Stripe Price ID for weekly subscription
# Find: Stripe Dashboard > Products > [Your Product] > Pricing > Price ID
NEXT_PUBLIC_STRIPE_PRICE_WEEKLY=

# =====================================================
# Google Maps Configuration
# =====================================================

# Google Maps API Key (server-side, for Geocoding/Directions)
# Create: Google Cloud Console > APIs & Services > Credentials
# Enable: Geocoding API, Directions API, Maps JavaScript API
GOOGLE_MAPS_API_KEY=

# Google Maps Map ID (for Maps JavaScript API)
# Create: Google Cloud Console > Maps > Map Management
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=

# Google Maps Browser Key (client-side, for displaying maps)
# Optional: Use a restricted key for browser requests
NEXT_PUBLIC_GOOGLE_MAPS_BROWSER_KEY=

# =====================================================
# Delivery Coverage Configuration
# =====================================================

# Comma-separated list of allowed counties for delivery
# Example: King County,Pierce County
COVERAGE_ALLOWED_COUNTIES=

# Maximum delivery distance in miles from kitchen
COVERAGE_MAX_DISTANCE_MILES=

# Maximum delivery duration in minutes
COVERAGE_MAX_DURATION_MINUTES=

# Coverage check cache TTL in minutes (reduces API calls)
COVERAGE_CACHE_TTL_MINUTES=

# =====================================================
# Cron & Background Jobs
# =====================================================

# Secret key for authenticating cron job requests
# Generate: openssl rand -base64 32
CRON_SECRET=
